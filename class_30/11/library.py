"""
import phonenumbers
x = phonenumbers.parse("+2348114994294", None)
print(x)
"""
#from bs4 import BeautifulSoup
#import requests
#import phonenumbers
#from phonenumbers import carrier

'''
def check_numbers(number):
    country_format = phonenumbers.format_number(phonenumbers.parse(
        number, "NG"), phonenumbers.PhoneNumberFormat.INTERNATIONAL)
    number_profile = phonenumbers.parse(country_format, "NG")
    telco = repr(carrier.name_for_number(number_profile, "en"))
    print("country Format: {} \nNumber profile: {} \nTelco: {}".format(
        country_format, number_profile, telco.strip("\'")))


check_numbers("+2348034378995")


# assignment: look into the library: Beautiful soup
#beautifulsoup is a good way to parse html text within python
'''


# pypi, pip:python installer package
# nodejs helps us to run java script; thats how javascript was incorporated into the backend
#shell scripting; cd desktop, mkdir, cd testdirectory, cd.. ,cls, 
